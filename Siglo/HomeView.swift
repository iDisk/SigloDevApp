//
//  Home.swift
//  Siglo
//
//  Created by Héctor Reyes on 19/12/24.
//

import SwiftUI

struct HomeView: View {
    var body: some View {
        NavigationView {
      /*      List(0..<10) { item in
                NavigationLink(destination: NewsDetailView()) {
                    HStack {
                        Image(systemName: "photo")
                            .resizable()
                            .frame(width: 50, height: 50)
                            .cornerRadius(8)
                        
                        VStack(alignment: .leading) {
                            Text("Título de la noticia \(item + 1)")
                                .font(.headline)
                            Text("Breve descripción de la noticia...")
                                .font(.subheadline)
                                .foregroundColor(.gray)
                        }
                    }
                }
            }
            .navigationTitle("Noticias") */
        }
    }
}

#Preview {
    HomeView()
}
